var React = require('react');
var Form = require('Form');

var Forecast = React.createClass ({
    render: function () {
    return (
      <div>
        <h3>Forecast </h3>
        <Form></Form>
    </div>
    );
  }
});

module.exports = Forecast;