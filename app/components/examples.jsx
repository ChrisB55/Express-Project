var React = require('react');
var {Link} = require('react-router');

var Examples = (props) => {
  return (
    <div>
      <h1 className="text-center page-title">Examples</h1>
      <p>Try entering New York or another major city.</p>
     
    </div>
  )
};

module.exports = Examples;
